<div id="intro">
  <div class="item background-cover" style="background: url(<%= image_path 'bg-featuredapp1.jpg' %>)">
    <div class="container">
      <div class="row">
        <div class="carousel-caption-center colour-white">
          <em>finally</em>
          <h1>Welcome <%= @user.name %></h1>
          <p>This is the first step.</p>
        </div>
      </div>
    </div>
    <div class="overlay-bg"></div>
  </div>
</div>

<section class="content text-center light">
  <div class="container">
    <div class="row">
      <h3>User</h3>
      <p>Name: <%= @user.name if @user.name %></p>
      <p>Email: <%= @user.email if @user.email %></p>
    </div>
  </div>
</section>
